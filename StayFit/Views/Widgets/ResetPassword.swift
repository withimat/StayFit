//
//  ResetPassword.swift
//  StayFit
//
//  Created by İmat Gökaslan on 18.12.2024.
//

import SwiftUI
/*
struct PasswordResetView: View {
    @StateObject private var viewModel = PasswordResetViewModel()
    
    var body: some View {
        NavigationStack {
            VStack(spacing: 20) {
                
                Text("Şifre Yenileme")
                    .font(.largeTitle)
                    .fontWeight(.bold)
                    .offset(y:30)
                Spacer()
                CustomTextField(ad: $viewModel.currentPassword,icon: "person",placeholder: "Eski Şifre")
                CustomTextField(ad: $viewModel.newPassword,icon: "person",placeholder: "Eski Şifre")
                
               
                
                if viewModel.isLoading {
                    ProgressView()
                } else {
                    Button(action: {
                        viewModel.resetPassword()
                    }) {
                        Text("Şifreyi Yenile")
                            .foregroundColor(.white)
                            .frame(maxWidth: .infinity)
                            .padding()
                            .background(Color.blue)
                            .cornerRadius(8)
                    }
                    .padding(.horizontal)
                }
                
               
                
            }
            .padding(.bottom)
            .background(Color("BG").opacity(0.7))
            
        }
    }
}


#Preview {
    PasswordResetView()
}
*/
